{{> mainmenu _id="home" }}

<section class="ui raised segment">
    {{> poi }}

    {{>error}}

    <div class="ui  divider"></div>
    <article class="eight wide column">
            <table id="poiTable" class="ui celled table segment">
                <thead>
                <tr>
                    <th>School Name</th>
                    <th>Address Line 1</th>
                    <th>County</th>
                    <th>Ethos</th>

                    <th>Image</th>

                </tr>
                </tr>
                </thead>
                <tbody>
                {{#each pois}}
                    <tr>
                        <td> <a href="/singlePoiDisplay/{{_id}}"  class="ui primary button">{{Off_Name}}</a> </td>
                        <td> {{Add_1}} </td>
                        <td> {{County}}</td>
                        <td> {{Ethos}} </td>

                        <td><div class="uk-card uk-card-default uk-card-small uk-text-center uk-text-baseline uk-animation-scale-up">
                            <div class="uk-card-media-top">
                                <img src="{{cloudinary_secure_url}}">
                            </div></td>

                    </tr>
                {{/each}}
                </tbody>
            </table>
        </article>
    </div>
</section>
